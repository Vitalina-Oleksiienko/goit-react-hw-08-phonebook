const styles = {
  container: {
    textAlign: "center",
  },
  title: {
    fontWeight: 500,
    fontSize: 48,
  },
};

const HomeView = () => (
  <div style={styles.container}>
    <h1 style={styles.title}>Hello! Please register!</h1>
  </div>
);

export default HomeView;
